name: Build DMG

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
        
    - name: Build with xcodebuild
      run: |
        xcodebuild -project "ILoveSkibidi V2.xcodeproj" \
          -target "Mixky" \
          -configuration Release \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          clean build
        
    - name: Create DMG
      run: |
        # Créer le dossier pour l'app
        mkdir -p dmg-content
        
        # Copier l'app buildée
        if [ -d "build/Build/Products/Release/Mixky.app" ]; then
          cp -R "build/Build/Products/Release/Mixky.app" dmg-content/
        elif [ -d "build/Release/Mixky.app" ]; then
          cp -R "build/Release/Mixky.app" dmg-content/
        else
          find build -name "*.app" -exec cp -R {} dmg-content/ \;
        fi
        
        # Créer le DMG
        hdiutil create -volname "Mixky" \
          -srcfolder dmg-content \
          -ov \
          -format UDZO \
          "Mixky-$(date +%Y%m%d-%H%M%S).dmg"
          
    - name: Upload DMG artifact
      uses: actions/upload-artifact@v4
      with:
        name: Mixky-DMG
        path: "*.dmg"
        retention-days: 30
        
    - name: Create Release
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        files: "*.dmg"
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
